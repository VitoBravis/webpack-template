mixin card(props)
    .card&attributes(attributes)
        .card__front.card__side
            img()
        .card__back.card__side
            img(src="https://deckofcardsapi.com/static/img/back.png")
